# debug: true
spring:
    jpa:
        show-sql: true
        hibernate.ddl-auto: update
    datasource:
        driver-class-name: org.postgresql.Driver

    #     url: ${DATABASE_URL}
        # username: sa
        # password: password